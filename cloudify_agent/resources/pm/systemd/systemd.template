[Unit]
Description=Cloudify Agent Worker Service

[Service]
TimeoutStartSec=0
Restart=on-failure
EnvironmentFile={{ config_path }}
User={{ user }}
Group={{ user }}
ExecStart={{ virtualenv_path }}/bin/worker --queue {{ queue }}

[Install]
WantedBy=multi-user.target
