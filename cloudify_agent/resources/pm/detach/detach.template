#!/bin/bash -e

# sourcing the configuration file
. {{ config_path }}

# running the celery worker command directly
{{ virtualenv_path }}/bin/python -m cloudify_agent.worker \
    --queue "{{ queue }}" \
    --max-workers {{ max_workers }} \
    --name "{{ name }}"
