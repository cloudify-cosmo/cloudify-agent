tosca_definitions_version: cloudify_dsl_1_5

imports:
    - https://dell-fusion.s3.eu-west-1.amazonaws.com/spec/cloudify/7.0.5/types.yaml

node_templates:
  node:
    type: cloudify.nodes.Compute
    properties:
      os_family: windows
      agent_config:
        install_method: plugin
