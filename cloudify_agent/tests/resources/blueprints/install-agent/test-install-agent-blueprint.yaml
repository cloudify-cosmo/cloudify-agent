tosca_definitions_version: cloudify_dsl_1_5

imports:
    - http://cloudify.co/spec/cloudify/7.0.3/types.yaml

node_templates:
  node:
    type: cloudify.nodes.Compute
    properties:
      agent_config:
        install_method: plugin
